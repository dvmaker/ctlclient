const ddd = (prefix) => {
	return `
	
  ~  CTL CLIENT


  ~ 👑 DDD CONSULTADO
  
  
  ~ 👑 Estado: ${ctl.resultado.estado}
  
  
  ~ 👑 Cidades: ${ctl.resultado.cidades}
  
  `
}

exports.ddd = ddd
